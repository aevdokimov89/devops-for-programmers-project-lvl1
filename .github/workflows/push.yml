name: Test CI 

on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Test CI
      run: make ci
  build:
    runs-on: ubuntu-latest
    steps:
     #- uses: actions/checkout@v1
     -name: Build       
      run: make build
      #run: make push
